# Base URL of the FastAPI backend (app/server.py). Defaults to this value
# even without a .env.local file — only needed if you're running the
# backend somewhere other than localhost:8001.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8001
